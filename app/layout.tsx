import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Bharatanatyam Artist | Professional Dancer in Jersey City',
  description: 'Professional Bharatanatyam dancer based in Jersey City, NJ. Explore my journey through classical Indian dance.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}